# prisma tutorial
```shell
# formats prisma schema 
npx prisma format
# migrate db
npx prisma migrate dev --name init 
# install cli
npm i @prisma/client 
# generate prisma cli
npx prisma generate
```